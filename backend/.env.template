# Copy this file to backend/.env and set a real key, e.g.:
#   openssl rand -hex 32
# When set, every endpoint (including the /v1 proxy) requires this key in an
# X-API-Key header. Leave unset to run without authentication (local dev only).
COLOMA_API_KEY=
